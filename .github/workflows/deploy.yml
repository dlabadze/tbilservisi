name: Deploy to Odoo Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, odoo-server]
    steps:
      - name: Pull and deploy
        run: /home/fmg/bin/odoo-git-deploy.sh
