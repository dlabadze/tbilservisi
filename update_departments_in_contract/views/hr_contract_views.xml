<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_open_update_contract_department_wizard" model="ir.actions.server">
        <field name="name">Update Department</field>
        <field name="model_id" ref="hr_contract.model_hr_contract"/>
        <field name="binding_model_id" ref="hr_contract.model_hr_contract"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
action = env['hr.contract'].with_context(active_ids=records.ids).action_open_department_update_wizard()
        </field>
    </record>
</odoo>
