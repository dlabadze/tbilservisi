import os
import sys

# Add odoo to path if needed, but we can just use the environment if it's set up.
# Actually, I'll just write a script that I can run via odoo-bin if possible, 
# but I don't know the config.
# I'll try to find the analytic account via grep in any XML data files first.
