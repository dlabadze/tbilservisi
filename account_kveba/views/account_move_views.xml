<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_kveba" model="ir.ui.view">
        <field name="name">account.move.form.kveba</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_create_picking_chamowera"
                        string="ჩამოწერა"
                        type="object"
                        class="btn-primary"
                        invisible="state != 'posted'"/>
            </xpath>
        </field>
    </record>
</odoo>
